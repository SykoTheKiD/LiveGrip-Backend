language: python

sudo: required

python:
  - "2.7"

before_install:
  - ./build.sh -b travis
  - cd LiveGrip/

services:
  - mysql

install:
  - pip install -r requirements.txt

before_script:
  - mysql -e 'create database livegrip;'

script: python manage.py test

after_script:
  - cd ../build
  - ./build.sh -l digitalocean

notifications:
  email: false
  slack: livegrip:kOaXoIv0Y9o6wyTBQLgATmRI
